<?php

class Test
{
	public function first()
	{
	}

	// Place the cursor at the after the snippet "mu" and expand it
	// then leave Insert Mode with <Esc>
	// Finaly launch the command: put! =nr2char(10) | call UltiSnips#Anon("test")
	// A new line testppublic is added above the method
	// When it should actually only be test
	mu
}
